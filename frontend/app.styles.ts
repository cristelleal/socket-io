import { StyleSheet } from 'react-native';

export const headerTintColor = '#304255';

export default StyleSheet.create({
  header: { backgroundColor: '#f4ebde' },
  headerTitle: { fontWeight: '700', letterSpacing: 0.4 },
  card: { backgroundColor: '#fffaf2' },
});
